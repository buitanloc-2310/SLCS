// Local-only camera beauty processor. It transforms the outgoing camera track,
// so remote participants see the same light/color treatment as self preview.
export class LocalBeautyProcessor {
  constructor({fps=24,onState=()=>{}}={}){this.fps=fps;this.onState=onState;this.rawTrack=null;this.video=null;this.canvas=null;this.ctx=null;this.stream=null;this.track=null;this.raf=0;this.settings={preset:'off',smooth:0,light:0,color:0};}
  setSettings(next={}){this.settings={...this.settings,...next};this.onState({...this.settings});}
  filter(){const s=this.settings;const preset={off:'',natural:'brightness(1.03) contrast(.99) saturate(1.02)',bright:'brightness(1.10) contrast(1.01) saturate(1.04)',warm:'brightness(1.06) sepia(.08) saturate(1.08)',studio:'brightness(1.08) contrast(1.04) saturate(1.06)',cinema:'contrast(1.08) saturate(.92)',soft:'brightness(1.09) contrast(.96) saturate(1.03)'}[s.preset]||'';return [preset,s.light?`brightness(${1+Number(s.light)/500})`:'',s.color?`saturate(${1+Number(s.color)/400})`:'',s.smooth?`contrast(${1-Number(s.smooth)/1200})`:''].filter(Boolean).join(' ')||'none';}
  async start(rawTrack){await this.stop(false);this.rawTrack=rawTrack;if(!rawTrack||typeof document==='undefined'||!HTMLCanvasElement.prototype.captureStream)return rawTrack;const v=document.createElement('video');v.muted=true;v.playsInline=true;v.srcObject=new MediaStream([rawTrack]);await v.play();const c=document.createElement('canvas'),ctx=c.getContext('2d',{alpha:false,desynchronized:true});this.video=v;this.canvas=c;this.ctx=ctx;let last=0;const draw=(now=0)=>{if(!this.rawTrack||this.rawTrack.readyState==='ended')return;const min=1000/this.fps;if(now-last>=min){last=now;const w=v.videoWidth||1280,h=v.videoHeight||720;if(c.width!==w||c.height!==h){c.width=w;c.height=h}ctx.save();ctx.filter=this.filter();ctx.drawImage(v,0,0,w,h);ctx.restore()}this.raf=requestAnimationFrame(draw)};draw();this.stream=c.captureStream(this.fps);this.track=this.stream.getVideoTracks()[0]||rawTrack;this.track.contentHint='motion';return this.track;}
  async stop(stopRaw=false){if(this.raf)cancelAnimationFrame(this.raf);this.raf=0;try{this.video?.pause()}catch{};try{this.video&&(this.video.srcObject=null)}catch{};for(const t of this.stream?.getTracks?.()||[])try{t.stop()}catch{};if(stopRaw&&this.rawTrack)try{this.rawTrack.stop()}catch{};this.video=this.canvas=this.ctx=this.stream=this.track=null;if(stopRaw)this.rawTrack=null;}
}
